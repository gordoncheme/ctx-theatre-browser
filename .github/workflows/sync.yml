
name: Sync Events Daily

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run sync script
        run: python project.py --sync-only

      - name: Commit and push changes
        id: commit
        run: |
          git config --global user.email "github-actions@users.noreply.github.com"
          git config --global user.name "GitHub Actions"
          git add events.json
          git commit -m "Automated sync" || echo "No changes"
          git push

      # - name: Send success email
      #   if: success()
      #   uses: dawidd6/action-send-mail@v3
      #   with:
      #     server_address: smtp.gmail.com
      #     server_port: 587
      #     username: ${{ secrets.EMAIL_USERNAME }}
      #     password: ${{ secrets.EMAIL_PASSWORD }}
      #     subject: "Sync Events Daily workflow succeeded"
      #     to: "gordoncheme@gmail.com"
      #     from: "Theatre Sync Bot <${{ secrets.EMAIL_USERNAME }}>"
      #     body: |
      #       The "Sync Events Daily" GitHub Actions workflow completed successfully.
      #
      #       Repository: ${{ github.repository }}
      #       Run number: ${{ github.run_number }}
      #       Run URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
